# S32K148 EVB OpenOCD configuration

# Use CMSIS-DAP (OpenSDA) interface
interface cmsis-dap
transport select swd
adapter speed 4000

# Target is Cortex-M4
set CHIPNAME s32k148
source [find target/armv7m.cfg]

# Reset configuration
reset_config srst_only srst_nogate connect_assert_srst
